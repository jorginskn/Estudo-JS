const calc =  (a, b)=> {
    console.log(`Resultados
    Soma = ${a} + ${b} = ${ a + b } 
    subtração = ${a} - ${b} = ${a - b } 
    multiplicação = ${a} * ${b} = ${ a * b} 
    divisão  = ${a} / ${b} = ${a / b}`)
}

calc(5,5)